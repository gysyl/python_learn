我将调用 `planning-with-files` 技能以获取其完整的定义和说明，然后为您详细解读该技能的功能、生成的文件结构以及适用场景。

**计划步骤：**
1.  调用 `Skill` 工具加载 `planning-with-files`，获取详细信息。
2.  根据加载的信息，向您介绍该技能的：
    *   **核心功能**：基于文件的任务规划与状态追踪。
    *   **工作流**：如何通过 `task_plan.md`, `findings.md`, `progress.md` 管理复杂任务。
    *   **适用场景**：何时应该使用此技能（如长任务、调研任务）。