# 所有 Skills 功能完整总结

> 本文档总结了项目中所有技能的详细功能和使用方法

最后更新：2026-01-18

---

## 📊 技能总览

当前项目共有 **4 个**技能：

| 技能名称 | 功能描述 | 主要用途 | 位置 |
|---------|---------|---------|------|
| `git-auto-push` | Git 自动提交推送 | 一键完成 git add + commit + push | `.claude/skills/` |
| `frontend-design` | 前端界面设计 | 创建高质量的前端界面和组件 | `.trae/skills/` |
| `skill-creator` | 技能创建工具 | 创建和打包新的技能 | `.trae/skills/` |
| `algorithmic-art` | 算法艺术生成 | 使用 p5.js 创建生成艺术 | `.trae/skills/` |

---

## 🔧 技能详细说明

### 1. Git Auto Push（Git 自动提交推送）

**技能标识：** `git-auto-push`

**功能描述：** 一句话完成 `git add + commit + push` 的自动化技能，自动分析 diff 并生成中文提交信息

#### 核心功能

- ✅ **自动检测变更** - 识别新增、修改、删除的文件
- ✅ **智能分析 diff** - 理解代码改动的实际内容
- ✅ **自动选择类型** - 根据变更特征自动选择 feat/fix/refactor 等
- ✅ **生成中文提交信息** - 符合规范的中文提交消息
- ✅ **安全可控** - 执行前展示提交信息供确认
- ✅ **一键推送** - 自动推送到远程仓库

#### 使用方法

```
用户: 帮我提交推送
用户: 提交代码
用户: quick commit
用户: /git-auto-push
```

#### 执行流程

1. 检查 Git 状态 → 获取待提交文件
2. 分析 Diff 内容 → 理解改动含义
3. 生成提交信息 → feat/fix/refactor 等
4. 展示确认界面 → 用户确认后执行
5. Git Add + Commit + Push
6. 返回执行结果

#### 提交类型识别规则

| 变更特征 | 类型 | 示例 |
|---------|------|------|
| 新增功能/文件 | feat | `feat(utils): 新增计算函数` |
| 修复 bug | fix | `fix(auth): 修复登录超时问题` |
| 重构代码 | refactor | `refactor(user): 重构用户验证逻辑` |
| 文档更新 | docs | `docs(readme): 更新安装说明` |
| 代码格式调整 | style | `style: 统一代码缩进` |
| 性能优化 | perf | `perf(api): 优化数据库查询` |
| 测试相关 | test | `test(utils): 添加单元测试` |
| 构建/工具链 | chore | `chore: 更新依赖版本` |

#### 安全保证

- ❌ 绝不使用 `--force` 或 `--force-with-lease` 强制推送
- ❌ 绝不修改 Git 配置
- ✅ 推送前先 commit 成功
- ✅ 执行前必须获得用户确认

---

### 2. Frontend Design（前端界面设计）

**技能标识：** `frontend-design`

**功能描述：** 创建独特、生产级的高质量前端界面，避免通用的 AI 生成的审美风格

#### 核心功能

- ✅ **设计思维指导** - 在编码前理解上下文并确定大胆的美学方向
- ✅ **独特视觉风格** - 避免通用的 AI 生成审美（如 Inter 字体、紫色渐变等）
- ✅ **创意排版** - 使用独特、美丽的字体组合
- ✅ **色彩与主题** - 承诺一致的审美，使用 CSS 变量保持一致性
- ✅ **动画与交互** - 使用动画效果和微交互，优先使用 CSS-only 方案
- ✅ **空间构图** - 非对称布局、重叠、对角线流动等
- ✅ **背景与视觉细节** - 创建氛围和深度，而非默认的纯色

#### 设计原则

**在设计前需要思考：**
- **Purpose**（目的）：这个界面解决什么问题？谁使用它？
- **Tone**（基调）：选择极端风格 - 极简主义、混乱主义、复古未来主义、有机/自然、奢华/精致、俏皮/玩具般、杂志风格、野兽派/原始、装饰艺术/几何、柔和/粉彩、工业/实用主义等
- **Constraints**（约束）：技术要求（框架、性能、可访问性）
- **Differentiation**（差异化）：是什么让它难以忘怀？人们会记住的一件事是什么？

**关键原则：**
选择清晰的概念方向并精确执行。大胆的极繁主义和精致的极简主义都有效 - 关键在于意图性，而不是强度。

#### 使用场景

当用户要求构建以下内容时使用：
- Web 组件或页面
- 应用程序（网站、落地页、仪表板）
- React 组件
- HTML/CSS 布局
- 任何 Web UI 的样式/美化

#### 审美指南重点

1. **Typography（排版）**
   - 选择美丽、独特、有趣的字体
   - 避免通用字体（Arial、Inter、Roboto）
   - 选择令人意想不到、有特色的字体
   - 将独特的显示字体与精致的正文字体配对

2. **Color & Theme（色彩与主题）**
   - 承诺一致的审美
   - 使用 CSS 变量保持一致性
   - 主导色配以鲜明点缀优于 timid、均匀分布的调色板

3. **Motion（动画）**
   - 使用动画制作效果和微交互
   - HTML 优先使用 CSS-only 解决方案
   - React 可用 Motion 库
   - 专注于高影响力时刻：一个精心编排的页面加载效果比分散的微交互更能带来愉悦

4. **Spatial Composition（空间构图）**
   - 意想不到的布局
   - 非对称、重叠、对角线流动
   - 破坏网格的元素
   - 慷慨的负空间或受控的密度

5. **Backgrounds & Visual Details（背景与视觉细节）**
   - 创建氛围和深度而非默认纯色
   - 添加与整体审美匹配的情境效果和纹理
   - 应用创意形式：渐变网格、噪声纹理、几何图案、分层透明度、戏剧性阴影、装饰边框、自定义光标、颗粒叠加

#### 永远避免

❌ 通用 AI 生成的审美：
- 过度使用的字体系列（Inter、Roboto、Arial、系统字体）
- 老套的配色方案（特别是白色背景上的紫色渐变）
- 可预测的布局和组件模式
- 缺乏特定情境特征的千篇一律设计

---

### 3. Skill Creator（技能创建工具）

**技能标识：** `skill-creator`

**功能描述：** 创建有效技能的指南，用于扩展 Claude 的专业能力

#### 核心功能

- ✅ **技能创建指导** - 提供创建新技能或更新现有技能的分步指南
- ✅ **最佳实践** - 介绍技能设计的核心原则和模式
- ✅ **结构模板** - 提供技能的解剖结构和组织方式
- ✅ **打包工具** - 自动验证和打包技能为 .skill 文件
- ✅ **渐进式披露** - 三级加载系统管理上下文效率

#### 什么是技能

技能是模块化、自包含的包，通过提供专业知识、工作流和工具来扩展 Claude 的能力。将它们视为特定领域或任务的"入职指南"——将 Claude 从通用代理转变为装备了没有任何模型完全具备的程序知识的专业代理。

#### 技能提供的内容

1. **Specialized workflows（专门工作流）** - 特定领域的多步骤程序
2. **Tool integrations（工具集成）** - 使用特定文件格式或 API 的说明
3. **Domain expertise（领域专业知识）** - 公司特定知识、架构、业务逻辑
4. **Bundled resources（打包资源）** - 脚本、参考和资产，用于复杂和重复性任务

#### 核心原则

**1. Concise is Key（简洁即关键）**
- 上下文窗口是公共品
- 技能与系统提示、对话历史、其他技能元数据和实际用户请求共享上下文窗口
- **默认假设：Claude 已经很聪明** - 只添加 Claude 没有的上下文
- 优先使用简洁示例而非冗长解释

**2. Set Appropriate Degrees of Freedom（设置适当的自由度）**
根据任务的脆弱性和可变性匹配特异性级别：

- **高自由度（基于文本的说明）**：当多种方法有效、决策取决于上下文或启发式方法指导方法时使用
- **中等自由度（伪代码或带参数的脚本）**：当存在首选模式、某些变化可接受或配置影响行为时使用
- **低自由度（特定脚本、少量参数）**：当操作脆弱且容易出错、一致性至关重要或必须遵循特定顺序时使用

**3. Anatomy of a Skill（技能解剖结构）**

每个技能由必需的 SKILL.md 文件和可选的打包资源组成：

```
skill-name/
├── SKILL.md (必需)
│   ├── YAML frontmatter 元数据 (必需)
│   │   ├── name: (必需)
│   │   └── description: (必需)
│   └── Markdown 说明 (必需)
└── 打包资源 (可选)
    ├── scripts/          - 可执行代码（Python/Bash/等）
    ├── references/       - 打算根据需要加载到上下文中的文档
    └── assets/           - 输出中使用的文件（模板、图标、字体等）
```

#### 技能创建流程

1. **通过具体示例理解技能**
   - 明确技能应支持的功能
   - 收集具体使用示例

2. **规划可重用的技能内容**
   - 分析每个示例以确定需要哪些脚本、参考和资产

3. **初始化技能**
   - 运行 `init_skill.py` 脚本生成模板

4. **编辑技能**
   - 从可重用资源开始（scripts/、references/、assets/）
   - 更新 SKILL.md

5. **打包技能**
   - 运行 `package_skill.py` 验证并打包为 .skill 文件

6. **迭代**
   - 在实际任务中使用
   - 根据反馈改进

#### 资源类型说明

**Scripts（scripts/）**
- **何时包含**：当重复重写相同代码或需要确定性可靠性时
- **示例**：`scripts/rotate_pdf.py` 用于 PDF 旋转任务
- **好处**：Token 高效、确定性、可在不加载到上下文的情况下执行

**References（references/）**
- **何时包含**：对于 Claude 在工作时应该参考的文档
- **示例**：财务架构、公司 NDA 模板、公司政策、API 规范
- **好处**：保持 SKILL.md 精简，仅在 Claude 确定需要时加载
- **最佳实践**：如果文件很大（>10k 字），在 SKILL.md 中包含 grep 搜索模式

**Assets（assets/）**
- **何时包含**：当技能需要在 Claude 产生的输出中使用的文件时
- **示例**：品牌资产、PowerPoint 模板、HTML/React 样板、字体
- **好处**：将输出资源与文档分离，使 Claude 能够使用文件而不将它们加载到上下文中

#### 渐进式披露设计原则

技能使用三级加载系统来有效管理上下文：

1. **元数据（name + description）** - 始终在上下文中（~100 字）
2. **SKILL.md 主体** - 当技能触发时（<5k 字）
3. **打包资源** - 根据 Claude 需要（无限，因为脚本可以在不读取到上下文窗口的情况下执行）

**关键原则：** 当技能支持多个变体、框架或选项时，在 SKILL.md 中仅保留核心工作流和选择指导。将特定于变体的细节（模式、示例、配置）移动到单独的参考文件中。

#### 打包和验证

```bash
# 打包技能
scripts/package_skill.py <path/to/skill-folder>

# 指定输出目录
scripts/package_skill.py <path/to/skill-folder> ./dist
```

打包脚本将：
1. **自动验证**技能，检查：
   - YAML frontmatter 格式和必需字段
   - 技能命名约定和目录结构
   - 描述的完整性和质量
   - 文件组织和资源引用

2. **打包**技能（如果验证通过），创建以技能命名的 .skill 文件（例如 `my-skill.skill`）

---

### 4. Algorithmic Art（算法艺术生成）

**技能标识：** `algorithmic-art`

**功能描述：** 使用 p5.js 创建算法艺术，具有种子随机性和交互式参数探索

#### 核心功能

- ✅ **算法哲学创建** - 生成计算美学运动的宣言
- ✅ **p5.js 实现** - 创建交互式生成艺术
- ✅ **种子随机性** - Art Blocks 模式的可重现生成
- ✅ **参数探索** - 实时调整参数探索变体
- ✅ **单文件输出** - 自包含的 HTML 伪影

#### 工作流程（两步）

**第一步：算法哲学创建（.md 文件）**
- 创建一个算法哲学（不是静态图像或模板）
- 通过以下方式表达：计算过程、涌现行为、数学之美
- 使用种子随机性、噪声场、有机系统
- 粒子、流、场、力
- 参数变化和受控混沌

**第二步：通过 p5.js 生成艺术表达（.html + .js 文件）**
- 基于创建的哲学实现算法
- 创建交互式查看器
- 实现参数控制

#### 算法哲学创建指南

**命名运动（1-2 个词）**
- "Organic Turbulence"（有机湍流）
- "Quantum Harmonics"（量子谐波）
- "Emergent Stillness"（涌现静止）

**阐述哲学（4-6 段落 - 简洁但完整）**

要捕获算法本质，表达该哲学如何通过以下方式体现：
- 计算过程和数学关系？
- 噪声函数和随机性模式？
- 粒子行为和场动力学？
- 时间演化和系统状态？
- 参数变化和涌现复杂性？

**关键指南：**
- **避免冗余**：每个算法方面应该只提及一次
- **反复强调工艺**：哲学必须多次强调最终算法应该看起来像是经过无数小时开发，经过精心提炼，来自该领域的绝对顶尖人士
- **留出创意空间**：对算法方向足够具体，但足够简洁，以便下一个 Claude 有空间进行极其高水平的解释性实现选择

#### 技术要求

**种子随机性（Art Blocks 模式）**
```javascript
let seed = 12345; // 或来自用户输入的哈希
randomSeed(seed);
noiseSeed(seed);
```

**参数结构**
```javascript
let params = {
  seed: 12345,  // 始终包含种子以保证可重现性
  // 添加控制 YOUR 算法的参数：
  // - 数量（多少？）
  // - 比例（多大？多快？）
  // - 概率（多可能？）
  // - 比率（什么比例？）
  // - 角度（什么方向？）
  // - 阈值（何时行为改变？）
};
```

**核心算法 - 表达哲学**
- 算法哲学应该决定构建什么
- 避免思考"应该使用哪个模式？"，而是思考"如何通过代码表达这个哲学？"

#### 工艺要求

**关键**：要达到精通水平，创建感觉像是通过无数次迭代由大师级生成艺术家提炼而出的算法。仔细调整每个参数。确保每个模式都有目的地涌现。这不是随机噪声 - 这是通过深度专业知识精炼的受控混沌。

- **平衡**：复杂性而没有视觉噪声，秩序而没有僵化
- **色彩和谐**：深思熟虑的调色板，而非随机 RGB 值
- **构图**：即使在随机性中，也要保持视觉层次和流动
- **性能**：流畅执行，如果动画则针对实时优化
- **可重现性**：相同的种子始终产生相同的输出

#### 输出格式

1. **算法哲学** - 作为 markdown 或文本解释生成美学
2. **单 HTML 伪影** - 从 `templates/viewer.html` 构建的自包含交互式生成艺术

#### 交互式伪影创建

**关键特性：**

**1. 参数控制**
- 数值参数的滑块（粒子数量、噪声比例、速度等）
- 调色板的颜色选择器
- 参数更改时实时更新
- 恢复默认值的重置按钮

**2. 种子导航**
- 显示当前种子号
- "Previous"和"Next"按钮循环种子
- "Random"按钮用于随机种子
- 输入字段跳转到特定种子
- 生成 100 个变体（种子 1-100）

**3. 单伪影结构**
- 所有内容内联
- p5.js 从 CDN 加载
- 无需外部文件（除 p5.js CDN）
- 立即在 claude.ai 或任何浏览器中工作

**4. 实现细节 - 构建侧边栏**

侧边栏结构：
1. **Seed（固定）** - 始终完全按显示包含
2. **Parameters（可变）** - 为艺术创建控件
3. **Colors（可选/可变）** - 如果艺术需要可调颜色则包含
4. **Actions（固定）** - 始终完全按显示包含

#### 使用场景

当用户请求以下内容时使用：
- 使用代码创建艺术
- 生成艺术
- 算法艺术
- 流场
- 粒子系统

#### 重要原则

- **ALGORITHMIC PHILOSOPHY（算法哲学）**：创建通过代码表达的计算世界观
- **PROCESS OVER PRODUCT（过程重于产品）**：始终强调美从算法的执行中涌现 - 每次运行都是独特的
- **PARAMETRIC EXPRESSION（参数表达）**：思想通过数学关系、力、行为交流 - 而非静态构图
- **ARTISTIC FREEDOM（艺术自由）**：下一个 Claude 以算法方式解释哲学 - 提供创意实现空间
- **PURE GENERATIVE ART（纯生成艺术）**：这是关于制作活着的算法，而不是具有随机性的静态图像
- **EXPERT CRAFTSMANSHIP（专家工艺）**：反复强调最终算法必须感觉精心制作，通过无数次迭代提炼，是计算美学领域绝对顶尖人士的深度专业知识的产物

---

## 📋 技能对比表

| 特性 | git-auto-push | frontend-design | skill-creator | algorithmic-art |
|-----|--------------|----------------|--------------|----------------|
| **主要用途** | Git 自动化 | 前端设计 | 技能开发 | 生成艺术 |
| **输出类型** | Git 提交 | HTML/CSS/JS | .skill 文件 | HTML/.md/.js |
| **交互性** | 低（确认后执行） | 高（可交互 UI） | 低（工具类） | 高（参数探索） |
| **创意性** | 低 | 高 | 中 | 高 |
| **技术栈** | Git | React/Vue/HTML | Python/MD | p5.js |
| **学习曲线** | 简单 | 中等 | 中等 | 较高 |
| **自动化程度** | 全自动 | 半自动 | 工具辅助 | 半自动 |

---

## 🎯 使用建议

### 何时使用哪个技能

**git-auto-push**
- ✅ 日常开发的快速提交
- ✅ 需要保持提交信息规范的团队协作
- ✅ 减少重复性 Git 操作

**frontend-design**
- ✅ 创建 Web 应用界面
- ✅ 设计落地页或仪表板
- ✅ 构建 React 组件
- ✅ 需要独特视觉风格的项目

**skill-creator**
- ✅ 创建新的自定义技能
- ✅ 打包现有技能供分享
- ✅ 学习技能设计最佳实践
- ✅ 验证技能结构

**algorithmic-art**
- ✅ 创作生成艺术作品
- ✅ 探索算法美学
- ✅ 创建交互式艺术体验
- ✅ 学习 p5.js 和生成艺术

---

## 📚 相关资源

### 技能文件位置

```
python_learn/
├── .claude/
│   └── skills/
│       └── git-auto-push/
│           ├── SKILL.md
│           ├── README.md
│           ├── examples/
│           ├── resources/
│           └── scripts/
└── .trae/
    └── skills/
        ├── frontend-design/
        │   └── SKILL.md
        ├── skill-creator/
        │   └── SKILL.md
        └── algorithmic-art/
            ├── SKILL.md
            └── templates/
                ├── viewer.html
                └── generator_template.js
```

### 学习资源

- [Claude Code 文档](https://docs.anthropic.com/claude/docs/claude-code)
- [p5.js 官方文档](https://p5js.org/reference/)
- [Git Conventional Commits](https://www.conventionalcommits.org/)

---

## 🔄 维护历史

- **v1.0** (2026-01-18): 初始版本，总结所有 4 个技能的功能

---

**文档维护者：** 项目维护团队
**最后更新：** 2026-01-18
